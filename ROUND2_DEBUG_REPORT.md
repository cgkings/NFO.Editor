# Qt6 第二轮调试与修复报告

## 新增：本地预告片目录

设置窗口新增“本地预告片设置”。配置文件字段：

```json
{
  "trailer": {
    "local_directory": "D:\\Trailers"
  }
}
```

点击预告片按钮时按以下顺序播放：

1. 设置目录中以番号命名的视频；
2. 当前影片目录中名称包含 `trailer` 的视频；
3. 网络预告片 API。

匹配支持大小写和常见分隔符差异，例如 `ABC-123` 可匹配 `ABC-123.mp4`、`ABC123.mkv`、`ABC-123-trailer.strm`，但不会误匹配 `ABC-1234.mp4`。

## 本轮继续修复

- 配置文件改为临时文件写入后原子替换，降低断电或程序中断造成 JSON 损坏的风险。
- 配置合并和编辑改用深拷贝，避免默认嵌套配置被意外修改。
- `winshell` 改为条件导入，缺失时给出明确错误，不再导致整个程序导入失败。
- 主窗口类正式命名为 `NFOEditorQt6`，保留 `NFOEditorQt5` 别名兼容旧调用。
- 移除 Qt6 不支持的 QSS `cursor: pointer` 属性，消除运行期警告。
- 本地预告片和影片目录预告片选择改为确定性排序。

## 已执行验证

- 全部 Python 文件语法编译。
- pyflakes 静态检查。
- 9 项纯逻辑单元测试。
- PySide6 offscreen 主窗口与设置窗口创建/关闭测试。
- 裁剪、查重、照片墙、批量改名四个工具窗口创建/关闭测试。
- 临时 NFO 媒体库异步加载、树选择和字段加载测试。
- 本地预告片设置到 `mpvnet` 调用的端到端测试。

Windows 上首次使用仍建议先用媒体库副本验证外部播放器、回收站和真实磁盘移动行为。
